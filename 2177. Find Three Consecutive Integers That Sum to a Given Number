class Solution {
public:
    vector<long long> sumOfThree(long long num) {
        if((num - 3) % 3) return {};
        long long first = (num - 3) / 3;
        return {first, first + 1, first + 2};
    }
};
