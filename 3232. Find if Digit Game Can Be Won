class Solution {
public:
    bool canAliceWin(vector<int>& nums) {
        int bal = 0;
        for(int i = 0; i < nums.size(); i++) {
            if(nums[i] < 10) {
                bal += nums[i];
            } else {
                bal -= nums[i];
            }
        }
        return bal;
    }
};
