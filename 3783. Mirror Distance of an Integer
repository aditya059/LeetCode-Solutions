class Solution {
    int reverse(int n) {
        int rev = 0;
        for(int i = n; i > 0; i /= 10) {
            rev = rev * 10 + (i % 10);
        }
        return rev;
    }
public:
    int mirrorDistance(int n) {
        return abs(n - reverse(n)); 
    }
};
