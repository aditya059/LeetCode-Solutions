class Solution {
public:
    vector<string> cellsInRange(string s) {
        vector<string> ans;
        for(char ch1 = s[0]; ch1 <= s[3]; ch1++) {
            for(char ch2 = s[1]; ch2 <= s[4]; ch2++) {
                ans.push_back(string(1, ch1) + string(1, ch2));
            }
        }
        return ans;
    }
};
