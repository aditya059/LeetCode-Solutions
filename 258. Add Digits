// C++
class Solution {
public:
    int addDigits(int num) {
        while(num > 9)
            num = addDigits(num / 10) + num % 10;
        return num;
    }
};


// Golang
func addDigits(num int) int {
    for num > 9 {
        num = addDigits(num / 10) + num % 10
    }
    return num
}
